apiVersion: v1
kind: Service
metadata:
  name: devops-info
  namespace: devops-lab09
  labels:
    app: devops-info
    app.kubernetes.io/name: devops-info
spec:
  type: NodePort
  selector:
    app: devops-info
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
